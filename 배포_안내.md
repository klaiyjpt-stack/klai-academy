# amyschool.kr — 배포 상태

## ✅ 완료 (2026-08-07 새벽)
1. **Vercel 배포**: 프로젝트 `site` (계정 klaiyjpt-4026). 라이브 `https://site-tau-sable-33.vercel.app`
2. **Vercel 도메인 추가**: amyschool.kr + www.amyschool.kr (Production)
3. **가비아 DNS 저장**:
   - `A @ 216.198.79.1`
   - `CNAME www c7775e803c351dcc.vercel-dns-017.com.`

→ DNS 전파되면 `https://amyschool.kr` 자동 오픈(HTTPS 자동). Vercel Domains에서 `Valid Configuration` 확인.

## ⚠️ 남은 것 (보안 — 곧)
**롸이팅 앱 관리자/선생님 페이지가 잠금 없이 공개됨.** 학생·외부인 접근 가능.
→ PIN 게이트 걸어야 함. 수정 후 아래로 재배포:
```bash
cd "/Users/baesungmi/개발/klai homepage/site" && npx vercel deploy --prod --yes
```

---
## 재배포 방법 (내용 수정 시)
```bash
cd "/Users/baesungmi/개발/klai homepage/site" && npx vercel deploy --prod --yes
```

## (참고) 아래는 최초 설정 기록
사이트 파일: `klai homepage/site/` (index.html + write/ 롸이팅첨삭앱)

---

## 1) 배포 (사이트를 인터넷에 올림)

터미널에서:

```bash
cd "/Users/baesungmi/개발/klai homepage/site" && npx vercel deploy --prod --yes
```

- 이미 Vercel 로그인돼 있음(계정 `klaiyjpt-4026`). 처음이면 프로젝트 이름만 엔터로 넘기면 됨.
- 끝나면 `https://xxxx.vercel.app` 주소가 나옴 → 브라우저로 열어 홈·"✍️ 롸이팅 첨삭" 링크 동작 확인.

> 참고: Claude가 자동 배포하려 했으나 "외부 공개" 안전장치에 막힘(정상). 이 명령은 네가 직접 실행하면 통과된다.

---

## 2) Vercel에 도메인 등록

1. https://vercel.com → 방금 만든 프로젝트 → **Settings → Domains**
2. `amyschool.kr` 입력 → Add
3. Vercel이 넣으라는 DNS 값을 보여줌 (아래 3번 값과 동일)

---

## 3) 가비아 DNS 연결 (도메인 → 사이트)

My가비아 → **서비스 관리 → DNS 관리툴 → amyschool.kr** 에서 레코드 추가:

| 타입 | 호스트 | 값 |
|------|--------|-----|
| A | @ | `76.76.21.21` |
| CNAME | www | `cname.vercel-dns.com.` |

- 저장 후 몇 분~수십 분이면 `https://amyschool.kr` 접속됨(HTTPS 자동).
- Vercel Domains 화면에 `Valid Configuration` 뜨면 완료.

---

## 현재 상태
- 도메인 amyschool.kr: 등록됨(가비아, 2029-07-30까지). **아직 어떤 사이트에도 연결 안 됨** = 정상, 위 3단계로 연결.
- 웹호스팅 따로 안 삼 → Vercel 무료로 대체(월 0원, HTTPS 포함).

## 나중 (요청 시)
- 로그인 버튼 = placeholder. 학습포털 만들 때 연결.
- 이벤트·블로그·프로그램링크(엘리아영어 등) = 예시 문구/빈 링크. 실제 내용·주소 넣기.
