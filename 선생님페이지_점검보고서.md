# 선생님 페이지 점검 보고서

**대상** `site/portal/teacher.html` (2026-08-18 기준) · 참고 `klai-portal/supabase/schema.sql`, `site/api/teacher-auth.js`
**작성** 원어민 교사용 가이드 제작 과정에서 발견. 모든 항목은 **코드 원문 근거**를 붙였습니다. 추정 항목은 "미확인"으로 따로 표시했습니다.
**검증 방법** 코드 정독 + Chromium 실제 구동 재현 + PostgreSQL 16에 `schema.sql`을 그대로 적용해 앱과 동일한 쿼리 재현. Supabase 실서버는 이 환경에서 네트워크가 차단되어 **조회하지 않았습니다.**

---

## 0. 오늘 안에 할 것 (우선순위 5개)

| # | 할 일 | 이유 |
|---|---|---|
| 1 | `/portal/` 경로를 외부 접근에서 차단 (Vercel Deployment Protection) | 학생 79명의 한글 실명·코드가 지금 인터넷에 공개돼 있음 (§1) |
| 2 | Supabase에서 `class_records`·`class_daily` 의 RLS 상태 확인 | RLS가 꺼져 있으면 anon 키만으로 전부 열람·수정·삭제 가능 (§2) |
| 3 | 날짜 하드코딩 1줄 수정 | 내일부터 매일 전날 기록을 덮어씀 (§3) |
| 4 | 1220 코드 중복 해소 (차서은·차서연) | 해당 반 **저장 자체가 실패** (§4) |
| 5 | 기본값 3개(출결·태도·공개) 변경 | 손대지 않은 학생이 "출석·우수·공개"로 기록됨 (§5) |

---

## 1. 🔴 치명 — 학생 79명의 실명·코드가 공개 정적 파일에 들어 있음

**근거** `teacher.html` 의 `const ROSTER = { ... }` 블록. `S('정예은' 등 한글 실명, 4자리 코드, 영어이름)` 형태로 **2명 강사 / 19개 반 / 학생 79명**이 그대로 들어 있습니다.

로그인 화면은 잠금이 아닙니다. `function show(v){ ... classList.toggle('hidden', ...) }` — CSS 클래스만 바꿉니다. 인증 이전에 `ROSTER`, Supabase anon 키, 노션 워크스페이스 주소가 이미 DOM에 존재합니다. `배포_안내.md` 에 원장님이 직접 적어두신 그대로입니다 — *"선생님 페이지가 잠금 없이 공개됨. 학생·외부인 접근 가능."*

PIN 게이트(`site/api/teacher-auth.js`)는 만들어져 있으나 **어느 HTML에서도 호출하지 않습니다** (`grep -rn "teacher-auth" site/` → 참조 0건). 죽은 코드입니다.

미성년자 개인정보이므로 개인정보보호법 대상입니다.

**조치**
1. (즉시) `/portal/` 을 Vercel Deployment Protection 뒤로 넣거나 공개 도메인에서 분리
2. (즉시) `site/robots.txt` 에 `Disallow: /portal/` + 페이지에 `<meta name="robots" content="noindex,nofollow">` — **완화책일 뿐 차단이 아닙니다**
3. (근본) `ROSTER` 를 클라이언트에서 삭제하고, 로그인 후 `students` 테이블에서 RLS를 태워 불러오기
4. 이미 크롤링됐다고 가정하고 4자리 코드 교체 검토

---

## 2. 🔴 치명 — 페이지가 읽고 쓰는 테이블 2개가 스키마에 정의돼 있지 않음

**근거**
- `teacher.html`: `SB.from('class_records').upsert(rows,{onConflict:'teacher,class_name,date,eval,student_code'})`, `.from('class_daily').upsert(...)`
- `grep -n "class_records\|class_daily" klai-portal/supabase/schema.sql` → **일치 없음**
- `포털_로그인_설정.md`: *"⏳ 개인 기록(성적·출결·리포트) = Phase 2 (schema.sql 적용, 다음 단계)"*

즉 두 테이블은 (a) 아직 없거나 — 이 경우 **모든 저장이 실패** — (b) Supabase 콘솔에서 손으로 만들어졌습니다. **SQL 에디터로 만든 테이블은 RLS가 기본 OFF입니다.** `schema.sql` 의 `enable row level security` 목록에도 이 두 테이블은 없습니다.

**로컬 PostgreSQL 16 재현 결과**

| 설정 | 결과 |
|---|---|
| RLS OFF | anon 역할로 `SELECT` / `UPDATE` / `DELETE` 전부 성공 → 페이지 소스에 적힌 anon 키만으로 전 학생 기록·강사 일지 열람·수정·삭제 가능 |
| RLS ON, 정책 없음 | `ERROR: new row violates row-level security policy` → 모든 저장이 영구 실패 |

anon 키는 `teacher.html` 소스에 그대로 있고(공개용 키라 그 자체는 정상), 디코드하면 만료가 **2036-08-09** 입니다. anon 키는 **모든 테이블에 RLS가 켜져 있을 때만** 안전합니다.

**확인 방법** (Supabase SQL 에디터에서 실행)
```sql
select relname, relrowsecurity from pg_class
 where relname in ('class_records','class_daily');
select tablename, policyname, cmd, qual from pg_policies
 where tablename in ('class_records','class_daily');
```
`relrowsecurity` 가 `f` 로 나오면 **실제 유출 상태로 간주하고 anon 키를 즉시 회전**해 주세요.

**조치 — 두 테이블을 버전관리 안에서 RLS와 함께 생성**
```sql
create table class_records (
  id uuid primary key default gen_random_uuid(),
  teacher_id uuid not null default auth.uid() references profiles(id),
  student_id uuid not null references students(id) on delete cascade,
  class_id   uuid not null references classes(id),
  date date not null,
  eval score_type not null,
  attendance attend_status,
  homework boolean,
  score int, max int,
  attitude text check (attitude in ('excellent','good','needs_work')),
  published boolean not null default false,
  created_by uuid not null default auth.uid(),
  updated_at timestamptz not null default now(),
  unique (student_id, date, eval)
);
create table class_daily (
  id uuid primary key default gen_random_uuid(),
  teacher_id uuid not null default auth.uid() references profiles(id),
  class_id uuid not null references classes(id),
  date date not null,
  class_memo text,
  class_memo_published boolean not null default false,
  updated_at timestamptz not null default now(),
  unique (class_id, date)
);
-- 강사 일지는 별도 테이블로 분리 (아래 §8 참고)
create table teacher_logs (
  id uuid primary key default gen_random_uuid(),
  teacher_id uuid not null default auth.uid() references profiles(id),
  class_id uuid not null references classes(id),
  date date not null,
  log text,
  updated_at timestamptz not null default now(),
  unique (teacher_id, class_id, date)
);
alter table class_records enable row level security;
alter table class_daily   enable row level security;
alter table teacher_logs  enable row level security;

create policy cr_rw on class_records for all to authenticated
  using (teacher_id = auth.uid() or fn_is_admin())
  with check (teacher_id = auth.uid() or fn_is_admin());
create policy cd_rw on class_daily for all to authenticated
  using (teacher_id = auth.uid() or fn_is_admin())
  with check (teacher_id = auth.uid() or fn_is_admin());
create policy tl_rw on teacher_logs for all to authenticated
  using (teacher_id = auth.uid() or fn_is_admin())
  with check (teacher_id = auth.uid() or fn_is_admin());
```

---

## 3. 🔴 치명 — 날짜가 하드코딩돼 있어 매일 전날 기록을 덮어씀

**근거** `teacher.html`: `$('#dateSel').value='2026-08-18';`

오늘(8/18)은 우연히 맞습니다. **내일부터 매일 틀립니다.** 그리고 `date` 는 upsert 키의 일부입니다.

**재현 결과**
```
8/17 저장:  6/6점, 메모 '요약을 잘 썼음', published=true
같은 키로 재전송: 2/6점, 태도 2, 메모 없음, published=false
결과 →  2026-08-17 | 2점 | 태도 2 | 메모 비어 있음 | published f
>>> 17일 점수와 메모가 사라짐. 이력 없음, 경고 없음, 두 번째 행도 생기지 않음.
```
`schema.sql` 에 감사(audit) 테이블도 soft-delete도 없어 **복구 불가**입니다. `updated_at:new Date().toISOString()` 은 **클라이언트 시계**라 위조 가능하고 기기 시간이 틀리면 그대로 틀립니다. 누가 썼는지 기록하는 컬럼도 없습니다 — 나중에 학부모 분쟁이 생기면 입증 수단이 없습니다.

**조치**
```js
$('#dateSel').value =
  new Intl.DateTimeFormat('sv-SE',{timeZone:'Asia/Seoul'}).format(new Date());
```
`toISOString().slice(0,10)` 은 쓰지 마세요 — UTC라 오전 9시 이전 수업이 전날로 기록됩니다.
추가로 `created_by uuid not null default auth.uid()`, DB 트리거로 `updated_at` 관리, append-only 이력 테이블을 권장합니다.

---

## 4. 🔴 치명 — 형제자매 코드 중복으로 해당 반 저장이 통째로 실패

**근거** `teacher.html`
```js
'5:00 IB Y2 (1/2)': [S('Amy','1220','차서은'), S('Isabel','1220','차서연'), ...]
```
같은 반·같은 강사·**같은 코드 1220**. upsert 충돌 키는 `teacher,class_name,date,eval,student_code` 이므로 두 행의 키가 완전히 같습니다.

**PostgreSQL 16 재현 — 이 반의 6행 upsert 그대로 실행**
```
ERROR:  ON CONFLICT DO UPDATE command cannot affect row a second time
rows_stored = 0
```
→ **차서은·차서연·박도준·송주환·이로운·김유솔 6명 전원, 매 저장마다 아무것도 저장되지 않습니다.** 조용한 덮어쓰기가 아니라 전체 실패입니다.

**로스터 전수 조사** 학생 79명이 서로 다른 코드 66개만 사용. **13개 코드를 26명이 공유**하며, 그중 12쌍이 같은 성씨입니다 — 4자리 코드는 학생 식별자가 아니라 **가족 식별자**(보호자 번호 뒷자리로 추정)입니다.

| 코드 | 공유 학생 | 비고 |
|---|---|---|
| **1220** | 차서은 · 차서연 | **같은 반** — 저장 실패 |
| 6155 | 이채연 · 이준석 | 같은 강사, 다른 반 |
| 8017 | 임채아 · 임채우 | 같은 강사, 다른 반 |
| 1206 | 지우림 · 지이룸 | 같은 강사, 다른 반 |
| 4004 | 이규은 · 이준규 | 강사 다름 |
| 4404 | 최지운 · 송채윤 | **성씨도 다름** — 가족 코드 가정도 깨짐 |

추가로 `송채윤` 이 서로 다른 코드·다른 영어이름으로 두 번 등장합니다(강사 각각). 동일인인지 동명이인인지 로스터만으로는 판별 불가 — **확인 필요**.

**조치** 충돌 키를 코드가 아니라 `students.id` 로 바꾸세요(§2 SQL의 `unique (student_id, date, eval)`). 임시로는 1220 두 명에게 서로 다른 코드를 부여해야 그 반이 저장됩니다.

---

## 5. 🟠 높음 — 손대지 않은 학생이 "출석·우수·공개"로 기록됨

**근거** `renderRows()` 가 만드는 행 마크업
```js
<select class="att">   // ['Present','Late','Absent','Excused'] → index 0 = 출석
<select class="tae">   // ['Excellent','Good','Needs work'] → index 0 = 우수
<input type="checkbox" class="chk pub" checked>   // 공개 = 켜짐
<input type="checkbox" class="chk hw">            // 숙제 = 꺼짐
```
`dbSaveRecords` 가 이 값을 그대로 씁니다. **손대지 않은 명단 = 전원 출석·전원 우수·전원 공개.** 출결은 학원의 공식 기록이고, 태도는 아무도 내리지 않은 긍정 평가이며 §11(가이드 기준) 월말 리포트에 반영됩니다.

부가 문제 2가지
- `attitude` 를 **selectedIndex 정수**로 저장 (`attitude:tr.querySelector('.tae').selectedIndex`). 옵션 순서를 바꾸거나 번역을 손대면 **과거 데이터 전체의 의미가 조용히 바뀝니다.** 문자열 + CHECK 제약으로 저장하세요.
- 출결 어휘 불일치: 앱은 `ATT_CODES=['present','late','absent','excused']`, `schema.sql` 은 `attend_status as enum ('present','late','absent','makeup')`. 재현 결과 `select 'excused'::attend_status;` → `ERROR: invalid input value`. **사유결석 기록이 마이그레이션에서 전부 실패하거나 유실됩니다.** '사유결석(excused)'과 '보강(makeup)'은 다른 개념이니 용어부터 정하셔야 합니다.

**조치** 두 select 앞에 `<option selected disabled>—</option>` 을 넣어 미입력을 강제하고, 공개 기본값을 꺼짐으로 바꾸세요.

---

## 6. 🟠 높음 — 강사 선택 드롭다운이 인증을 대체하고 있음

**근거**
```js
<select id="teaSel" title="선생님 (데모)"></select>
let TEACHER = Object.keys(ROSTER)[0];     // 항상 'NEZ (Class 1)'
$('#teaSel').onchange = ()=>{ TEACHER=$('#teaSel').value; ... };
// 저장: const teacher=TEACHER, ...     // 로그인 계정이 아님
// 조회: .eq('teacher',TEACHER)
```
`TEACHER` 는 세션이 아니라 **드롭다운에서 고른 문자열**이며, 읽기·쓰기 양쪽의 유일한 권한 기준입니다. Rosemary 선생님이 로그인해도 화면은 NEZ의 10개 반으로 열리고, 그대로 수업을 기록하면 **NEZ 이름으로 저장**됩니다. 반대로 아무 강사나 동료를 선택해 동료의 기록과 **비공개 강사 일지**를 열 수 있습니다.

`title="선생님 (데모)"` 이라는 라벨도 오해를 부릅니다 — `if(!SUPABASE_URL || !SUPABASE_ANON)` 데모 분기는 두 상수가 채워져 있어 **죽은 코드**이고, 이 드롭다운은 실제 동작합니다. 게다가 `data-t` 가 없어 `applyLang()` 이 건드리지 않으므로 원어민에게는 **라벨 없는 상자**로 보입니다.

**조치** `#teaSel` 을 삭제(또는 `profiles.role` 기반 관리자 전용)하고 세션의 `auth.uid()` 에서 강사를 도출하세요. **클라이언트만 고쳐서는 의미가 없고 §2의 RLS가 함께 있어야 합니다.**

---

## 7. 🟠 높음 — 기존 RLS가 모든 강사에게 모든 학생의 민감 메모를 열어줌

**근거 + PostgreSQL 재현** (`schema.sql` 원문 적용, NEZ 계정으로 Rosemary 담당 학생 조회)

| 쿼리 | 정책 | 결과 |
|---|---|---|
| `select note from teacher_notes` | `tn_all for all using (fn_is_staff())` | `민감: 부모 별거, 수업 중 감정 불안` — **열람됨** |
| `delete from scores where ...` | `scores_write for all using (fn_is_staff())` | `DELETE 1` — **삭제됨** |
| `update students set korean_name=...` | `students_write for all using (fn_is_staff())` | **덮어써짐** |

`fn_is_staff()` 는 모든 강사에게 true를 반환하고 `for all` 은 SELECT/INSERT/UPDATE/DELETE 전부를 포함합니다. 즉 "🔒 민감 사항 = 원장·선생님만"은 **"모든 강사가 볼 수 있다"** 는 뜻이고, 어느 강사든 다른 강사의 성적을 조용히 지울 수 있습니다.

`fn_can_view_student()` 는 올바르게 작성돼 있으나 **쓰기에는 쓰이지 않고 `teacher_notes` 에는 아예 적용되지 않습니다.**

**조치**
```sql
drop policy tn_all on teacher_notes;
create policy tn_select on teacher_notes for select to authenticated
  using (fn_can_view_student(student_id));
create policy tn_write on teacher_notes for insert to authenticated
  with check (fn_can_view_student(student_id) and created_by = auth.uid());
create policy tn_update on teacher_notes for update to authenticated
  using (created_by = auth.uid() or fn_is_admin());
-- delete 정책은 두지 않음: 미성년자 기록은 강사가 지울 수 없어야 함

drop policy scores_write on scores;
create policy scores_write  on scores for insert to authenticated with check (fn_can_view_student(student_id));
create policy scores_update on scores for update to authenticated using (fn_can_view_student(student_id));
create policy scores_delete on scores for delete to authenticated using (fn_is_admin());

drop policy students_write on students;
create policy students_write on students for all to authenticated
  using (fn_is_admin()) with check (fn_is_admin());
```

또한 `classes_select ... using (true)` 는 `to authenticated` 가 없어 **로그아웃 상태(anon)에도 허용**됩니다. 재현 결과 세션 없이 반 목록과 담당 강사가 전부 조회됐습니다. `programs`, `books`, `class_homework` 도 같습니다. 네 곳 모두 `to authenticated` 를 붙이세요.

---

## 8. 🟠 높음 — 학부모 공개용 메모와 비공개 강사 일지가 같은 행에 있음

**근거** `dbSaveDaily()` 가 `class_memo`(학부모 공개용)와 `teacher_log`(비공개)를 **`class_daily` 한 행**에 씁니다. PostgREST에는 컬럼 단위 RLS가 없으므로, 나중에 학부모용 `select` 정책을 만드는 순간 강사 일지까지 함께 나갑니다. 개별 메모도 같은 구조입니다 — `note`, `note_type`, `note_public` 이 `class_records` 안에 `published` 와 나란히 있습니다.

`schema.sql` 에는 이미 제대로 잠긴 `teacher_notes` 테이블이 있는데 **앱이 자기 스키마를 통째로 우회**하고 있습니다(`scores`, `attendance`, `portfolio` 도 마찬가지).

**조치** `teacher_logs` 분리(§2 SQL) + 학부모 읽기는 기본 테이블이 아니라 뷰로
```sql
create view class_daily_public with (security_barrier) as
  select class_id, date, class_memo from class_daily where class_memo_published;
grant select on class_daily_public to authenticated;
```

---

## 9. 🟠 높음 — 로그인 시 역할(role) 검사가 전혀 없음

**근거**
```js
async function route(){ const { data:{ session } } = await sb.auth.getSession();
  if(session){ USER=session.user; ... boot(); show('dashView'); }
```
조건은 **"세션이 존재하는가"** 뿐입니다. `profiles.role` 을 읽지 않습니다. 학생·학부모 계정을 만드는 순간(= 바로 다음 단계) **누구나 `/portal/teacher.html` 에 로그인해 전체 명단·모든 강사의 기록·비공개 일지를 열 수 있습니다.**

**미확인** 이 Supabase 프로젝트에 이메일 자가가입이 켜져 있는지. 켜져 있으면(기본값 ON) 인터넷의 누구나 가입해 이 검사를 통과합니다. → Supabase Dashboard → Authentication → Providers → Email → "Enable Sign Ups" 확인.

**조치** `getSession()` 이후 `select role from profiles where id = auth.uid()` 로 `teacher/admin/director` 외에는 거부. 공개 가입 끄기. 클라이언트 검사는 예의일 뿐이니 **RLS에서도 강제**하세요.

---

## 10. 🟠 높음 — 언어 전환·반/날짜 변경이 입력 내용을 지움

**근거** `renderRows()` 가 `$('#rows').innerHTML = ...` 로 행 전체를 다시 만듭니다. 이 함수는 `applyLang()` 안에서도 호출되고, `applyLang()` 은 한국어/English 버튼에 연결돼 있습니다.

**Chromium 재현**
```
전: 결석 / 숙제O / 4점 / 노력요망 / 공개OFF / 메모 있음 / 영상 링크 있음
언어 버튼 클릭
후: 출석 / 숙제X / 공란 / 우수 / 공개ON / 메모 null / 영상 null
```
단순히 지워지는 게 아니라 **틀린 기본값으로 되돌아갑니다.** 결석·노력요망·비공개로 표시했던 학생이 한 번의 클릭으로 "출석·우수·공개" 직전 상태가 됩니다.

부가: `dbLoadRecords()` 는 `async` 인데 **await 하지 않습니다.** 로딩이 끝나기 전에 타이핑을 시작하면 DB 값이 입력을 덮어씁니다.

**조치** 그리드를 상태 객체로 직렬화해 그 객체에서 다시 그리도록 바꾸세요. 최소한 4개 트리거 전부에 dirty 검사 + `confirm()` 을 넣고, `await dbLoadRecords()` 이후에 입력을 허용하세요.

---

## 11. 🟡 중간 — 성공하지 않았는데 성공했다고 표시함

**근거·재현** 공개 체크를 전부 해제하고 `save(true)` 실행 →
```
toast: "반 전체 저장·게시 완료 (데모 · 6명)"
```
한 줄에 결함이 3개입니다.
1. `const n=document.querySelectorAll('#rows tr').length` — **화면의 행 수**를 셉니다. 실제 게시된 건 0명인데 6명이라고 표시합니다. → `#rows .pub:checked` 로 바꿔야 합니다.
2. 성공 문구에 `데모`/`demo` 가 하드코딩돼 있습니다. 실제 저장에도 그대로 나와 선생님은 "저장 안 됐나?" 로 읽습니다.
3. `memoPublish()` 가 `dbSaveDaily()` 를 **await 하기 전에** `memoLocked=true; memoSync()` 를 실행합니다 → 실패해도 초록색 "✅ 발행됨"이 먼저 뜨고, 실패 토스트는 1.8초 뒤 사라집니다. `teaSave()` 도 동일.

추가: `dbLoadRecords()` 가 `{ data }` 만 구조분해하고 **error를 버립니다.** RLS 거부·테이블 없음·연결 끊김이 "아직 기록 없음"과 구분되지 않고, 그 상태에서 저장하면 멀쩡한 기록을 기본값으로 덮어씁니다.

---

## 12. 🟡 중간 — 그 밖의 확인된 항목

| 항목 | 근거 | 조치 |
|---|---|---|
| **바로 아래 가이드에 반영함** — 아이디가 이메일 전체여야 함 | `const ID_DOMAIN = "";` + `id.includes('@')?id:id+ID_DOMAIN`. placeholder 는 `e.g. anna`. 같은 프로젝트를 쓰는 `portal/index.html` 은 `ID_DOMAIN="@klai.kr"` 로 **서로 다릅니다** | `ID_DOMAIN` 을 `"@klai.kr"` 로 통일하고, "아이디 형식 오류"와 "비밀번호 틀림"을 구분해 표시 |
| CDN 차단 시 **완전 백지 화면** | `import('https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2/+esm')` 실패 시 두 뷰 모두 `hidden` 인 채로 남음. 재현 결과 화면 텍스트 길이 0 | try/catch + 오류 화면. 라이브러리를 `site/vendor/` 에 버전 고정으로 내려 동일 출처 제공 (SRI 문제도 함께 해결) |
| 공급망 위험 | `@2` 는 마이너 버전마다 바뀌는 부동 태그, SRI 해시 없음. 탈취되면 세션 토큰과 전 학생 기록 접근 가능 | 위와 동일 |
| 영상 링크에 **동의 기록 필드가 없음** | `$('#mVidPub').checked = dataset.vidpub!=='0'` → 신규 영상은 **공개 기본 ON**. URL 검증·허용 도메인 없음 | 기본 OFF로. `students` 에 `media_consent_on date` 추가 후 동의 없으면 `video_public=true` 를 막는 트리거. 장기적으로 Supabase Storage + 서명 URL |
| 자유 문자열을 키로 사용 | `teacher:'NEZ (Class 1)'`, `class_name:'3:00 IB Y1 (21/22)'`. 시간표가 바뀌거나 소스 주석대로 `⚠️철자/코드 확인 필요` 를 고치는 순간 과거 기록이 UI에서 사라지고 중복 행이 생김 | `classes.id` / `students.id` 로 FK. 표시 이름은 렌더 시점에 해석 |
| 비공개 일지가 `localStorage` 에 남음 | `memoStore()` 가 `oninput` 마다 강사 일지를 `klai_memo::...` 키로 저장. `logoutBtn` 은 `signOut()` 만 호출 | 로그아웃 시 `klai_` 로 시작하는 키 전부 삭제. 일지는 애초에 클라이언트에 저장하지 않기 |
| 잠재 저장형 XSS | `renderRows()` 가 `st.ko/st.en/st.code` 를 이스케이프 없이 `innerHTML` 에 넣음. 지금은 하드코딩이라 안전하지만 §1 조치로 DB에서 불러오는 순간 위험해짐 | 지금 미리 `createElement` + `textContent` 로 변경 |
| 미연결 스키마 위생 | `schema.sql` 에 `if not exists`/트랜잭션 없음 · `homework_done` 에 unique 없음 · `lesson_plan_images.is_public` 기본 **true** · `profiles_select` 가 모든 강사에게 모든 학부모·학생 프로필 노출 · `attendance` 의 `unique(student_id,date)` 는 하루 2회 평가와 충돌 | 마이그레이션 전에 정리 |
| 보존·파기 정책 없음 | 스키마 전체에 없음 | 미성년자 기록은 개인정보보호법상 보존기간·파기 절차가 필요합니다 |

---

## 13. 가이드 문서에 이미 반영한 것

원어민 가이드(v2)에는 위 항목 중 **선생님이 스스로 피할 수 있는 것**을 "Read this before you touch anything" 섹션과 각 단계 경고로 넣어두었습니다.

- 날짜를 매번 직접 설정 (§3)
- 강사 드롭다운을 본인 이름으로 (§3, §6)
- 저장 전에는 반·날짜·평가·**언어**를 바꾸지 말 것 (§10)
- 출결·태도 기본값, 숙제·공개 기본값 (§5)
- 1220 반은 종이로 기록하고 원장님께 보고 (§4)
- 동명이인은 4자리 코드로 확인
- 아이디는 이메일 전체 (§12)
- 저장 토스트의 "(demo)" 는 잔여 문구 (§11)
- 백지 화면이면 네트워크 문제 (§12)
- 빠뜨린 날짜를 나중에 채우지 말 것 — 손대지 않은 행이 "출석·우수·공개"로 저장되므로 **없는 출석을 만들어냄** (§5)
- 강사 일지는 다른 직원도 볼 수 있으므로 민감 내용 금지 (§8)
- 영상은 동의 확인 → 미등록(Unlisted) 업로드 → 공개 체크 해제 (§12)

**단, 문서는 코드 결함의 대체재가 아닙니다.** 위 §0의 5가지는 문서로 막을 수 없습니다.
