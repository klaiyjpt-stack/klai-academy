# 학생 로그인(포털) 활성화 — 원장님 설정 순서

포털/대시보드는 다 만들어져 있고, **Supabase 값 2개만 넣으면 로그인 작동**합니다.
지금은 값이 비어서 "대시보드 미리보기 모드"(노란 배너)로 보여요.

파일: `site/portal/index.html` 맨 위 `<script type="module">` 안 4줄.

## 1) Supabase 값 얻기
- 입시앱이 이미 쓰는 Supabase 재사용 권장(계정 공유).
- Supabase 대시보드 → 프로젝트 → **Settings → API**
  - **Project URL** → `SUPABASE_URL` 에 붙여넣기
  - **anon public** key → `SUPABASE_ANON` 에 붙여넣기 (⚠️ service_role 말고 **anon** — 공개해도 되는 키)

```js
const SUPABASE_URL  = "https://xxxx.supabase.co";   // ← 여기
const SUPABASE_ANON = "eyJhbGci...";                // ← 여기 (anon public)
const ID_DOMAIN     = "@klai.kr";                    // 학생 아이디에 붙는 내부 도메인 (그대로 둬도 됨)
const IPSI_STUDENT_URL = "https://ipsi-material-app.vercel.app/study";  // 입시앱 학생 진입 주소 (실제로 확인)
```

## 2) 학생 계정 만들기 (테스트 1명 먼저)
Supabase → **Authentication → Users → Add user**
- Email: `jimin01@klai.kr` (학생 아이디 + @klai.kr)
- Password: 원하는 비번
- → 학생은 로그인 화면에서 **아이디 `jimin01`** + 비번만 입력 (@klai.kr는 자동으로 붙음)
- 나중에 100명+ 는 일괄 생성 스크립트로 (요청 시 만들어드림)

## 3) 입시앱 학생 주소 확인
`IPSI_STUDENT_URL` = 입시앱에서 **학생이 보는 화면** 주소로. (admin 말고 study/s 등 학생 라우트)

## 4) 재배포
```bash
cd "/Users/baesungmi/개발/klai homepage/site" && npx vercel deploy --prod --yes
```

## 5) GitHub 저장 (비공개 확인 후)
```bash
cd "/Users/baesungmi/개발/klai homepage" && git add -A && git commit -m "학생 포털 로그인" && git push
```

---
## 지금 상태 (내가 해둔 것)
- ✅ 포털 로그인+대시보드 UI (`site/portal/`)
- ✅ 앱 4개 연결: 파닉스·사이트워드(사이트에 배포함) · Writing Lab · 입시자료(URL)
- ✅ 프로그램 링크: 클래스카드·리딩터치·클라이영어·엘리아영어
- ✅ 홈/수업/앱 페이지 "로그인" 버튼 → 포털 연결
- ⏳ Supabase 값·학생계정 = 위 1~2 (원장님)
- ⏳ 개인 기록(성적·출결·리포트) = Phase 2 (schema.sql 적용, 다음 단계)

## 참고
- 지금 앱들은 대시보드에서 링크로 열림. **딥링크 직접 접근 차단**(앱도 로그인 확인)은 Phase 2에서.
- 엘리아는 로그인 주소를 몰라 공식 홈(eleaenglish.com)으로 걸어둠 — 학습 로그인 주소 있으면 교체.
