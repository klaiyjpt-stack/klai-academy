# KLAI Portal — 전체 빌드 플랜

## 단계 (Phase)
### Phase 0 — 뼈대 (현재)
- [x] 프로젝트 구조, README, 이 플랜
- [x] Supabase 스키마 + RLS (`supabase/schema.sql`)
- [x] package.json, 환경변수, Supabase 클라이언트
- [ ] Next.js 초기화(next.config, tailwind, app/layout) — Claude Code에서 `npx create-next-app` 후 병합

### Phase 1 — 공개 홈페이지 + 로그인 뼈대
- [ ] `/` 공개 홈페이지 (학원 소개, 수업 과정 5개, 이벤트/블로그, 카카오 상담, 로그인 버튼)
- [ ] `/login` (Supabase Auth, 학원 배부 계정만)
- [ ] 로그인 후 역할별 리다이렉트

### Phase 2 — 학생/학부모 페이지
- [ ] `/student` 시험·평가, 출결(이코딩), 숙제, 내 학습과정, 프로그램 바로가기
- [ ] `/parent` 학습요약, 출결, 숙제, 수강료·결제(이코딩 링크), 레슨플랜, 포트폴리오
- [ ] RLS로 결제 비공개·본인 데이터만 검증

### Phase 3 — 선생님 페이지
- [ ] `/teacher` 구글시트형 반 전체 일괄 입력(개별/반별), 점수 맞은/총, 공개 스위치
- [ ] `/teacher/lesson-plan` 레슨플랜 생성기 이식 (교재 시리즈→권, 달력, 2레슨, 공휴일)
- [ ] 한국어/영어 전환(i18n)

### Phase 4 — 원장/관리자 페이지
- [ ] `/admin` 대시보드, 학생관리(반별·영역별 필터·엑셀), 반 편성·이동, 계정 생성·배부,
      프로그램 링크, 교재·재고·주문, 수강료(이코딩), 구성원·권한(부원장 대시보드별 권한 설정)

### Phase 5 — 연동 & 마무리
- [ ] 이코딩 출결·수납 엑셀 가져오기(CSV import) / 링크
- [ ] Google Sheets(월간레포트) 양방향(1단계 단방향부터)
- [ ] 카카오 채널 연결, PWA(설치·오프라인), 유튜브 임베드
- [ ] 데이터 이전(구글시트→Supabase), 권한 테스트, 성능 최적화

## 라우트 → 시안 매핑
| Route | 역할 | 시안(HTML) |
|---|---|---|
| `/` | 공개 | 클라이_공개홈페이지_시안.html |
| `/student` | 학생 | 클라이_학생페이지_시안.html |
| `/parent` | 학부모 | 클라이_학부모페이지_시안.html |
| `/teacher` | 선생님 | 클라이_선생님페이지_시안(.html/_영어) |
| `/teacher/lesson-plan` | 선생님 | klai_lesson_plan_generator.html |
| `/admin` | 원장·관리자 | 클라이_원장관리자페이지_시안.html |

## 반드시 체크(개발 필수)
1. RLS 권한 테스트(학생이 남의 것·결제 못 봄)  2. 알파 파이널→레벨 자동배정 매칭키(영어이름+숫자)  3. 입력 표준화  4. 선생님 입력 기본 비공개
